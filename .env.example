# Discord channel → Edit Channel → Integrations → Webhooks → New Webhook → Copy URL
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxxxx/yyyyy

# Optional. Raises the GitHub Search API rate limit from 10/min to 30/min.
# A token with no scopes is enough — this only reads public data.
GITHUB_TOKEN=
